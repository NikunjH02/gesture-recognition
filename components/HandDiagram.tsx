// import React from 'react';
// import { View, StyleSheet } from 'react-native';
// import Svg, { Path } from 'react-native-svg';

// const Finger = ({ isOpen }: { isOpen: boolean }) => (
//   <Path
//     d={isOpen ? "M10 10 L20 10 L20 50 L10 50 Z" : "M10 10 L20 10 L20 30 L10 30 Z"}
//     fill={isOpen ? "#00FF00" : "#FF0000"}
//   />
// );

// const HandDiagram = ({ values }: { values: number[] }) => (
//   <Svg height="100" width="100" viewBox="0 0 100 100">
//     <Finger isOpen={values[0] > 0.5 } />
//     <Finger isOpen={values[1] > 0.5 } />
//     <Finger isOpen={values[2] > 0.5 } />
//     <Finger isOpen={values[3] > 0.5 } />
//     <Finger isOpen={values[4] > 0.5 } />
//   </Svg>
// );

// export default HandDiagram;

// const styles = StyleSheet.create({
//   // Add any styles if needed
// });
